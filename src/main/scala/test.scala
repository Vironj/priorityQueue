object test extends App {
  var queue = new PriorityQueue[Int]
  queue.isEmpty
  queue.AddElement(4)
  queue.AddElement(3)
  queue.AddElement(7)
  queue.sizeOfTree
  queue.removeElement
  queue.sizeOfTree
}
